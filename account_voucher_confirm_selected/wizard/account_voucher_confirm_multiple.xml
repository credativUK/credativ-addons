<?xml version="1.0" encoding="UTF-8"?>
<openerp>
    <data>
        <record id="action_server_voucher_confirm_multiple" model="ir.actions.server">
            <field name="name">Confirm Selected</field>
            <field name="condition">True</field>
            <field name="model_id" ref="account_voucher.model_account_voucher"/>
            <field name="state">code</field>
            <field name="code">self.proforma_voucher(cr, uid, context.get('active_ids', []), context=context)</field>
        </record>

        <record model="ir.values" id="action_voucher_confirm_multiple">
            <field name="name">action_voucher_confirm_multiple</field>
            <field name="action_id" ref="action_server_voucher_confirm_multiple"/>
            <field name="value" eval="'ir.actions.server,' + str(ref('action_server_voucher_confirm_multiple'))"/>
            <field name="key">action</field>
            <field name="key2">client_action_multi</field>
            <field name="model_id" ref="account_voucher.model_account_voucher"/>
            <field name="model">account.voucher</field>
        </record>
    </data>
</openerp>
